export function getRandomInt(max) {
	return Math.floor(Math.random() * (max + 1));
}

export const reactDescriptions = ["Fundamental", "Crucial", "Core"];
export const tabOptions = ["Components", "JSX", "Props", "State"];
